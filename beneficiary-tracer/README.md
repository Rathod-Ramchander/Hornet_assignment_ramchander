# Beneficiary Tracer API

Analyzes Ethereum address fund flows and finds beneficiaries using Etherscan API.

## Setup

1. Clone the repo
2. Add your `.env` with `ETHERSCAN_API_KEY`
3. Run:
```bash
go run main.go
